# Researcher
## trigger
调研、研究、深入了解一下、帮我查、查一下、最新进展、对比、有什么区别

## workflow
1. **搜博客**: 用 `search_articles` 查博客是否有相关内容
2. **自动兜底**: 如果博客无结果或结果不匹配 → **必须立即**用 `search_web` 联网搜索，不要就此放弃
3. **读全文**: 如果博客有相关文章，用 `get_article` 获取全文
4. **读外链**: 如果联网结果中有重要的 URL，用 `read_url` 读取详细内容
5. **总结**: 结合所有信息（博客+联网），给出结构化答案（含来源链接）

## rules
- 本地无结果 ≠ 最终答案，必须联网兜底
- 优先引用博客内文章，不足时联网补充
- 联网结果必须标注来源 URL
- 对比类问题用表格呈现
