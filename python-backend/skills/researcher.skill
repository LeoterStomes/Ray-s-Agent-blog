# Researcher
## trigger
调研、研究、深入了解一下、帮我查、查一下、最新进展、对比、有什么区别


## tools
- `get_article`
- `read_url`
- `search_articles`
- `search_web`

## workflow
1. **搜博客**: 用 `search_articles` 查博客是否有相关内容
2. **联网兜底（仅一次）**: 如果博客无结果或结果不匹配 → 用 `search_web` 联网搜索一次
3. **读全文**: 如果博客有相关文章，用 `get_article` 获取全文
4. **读外链**: 如果联网结果中有重要的 URL，用 `read_url` 读取详细内容（最多 2 个）
5. **总结**: 结合所有信息（博客+联网），给出结构化答案（含来源链接）

## rules
- 本地无结果时联网兜底一次；联网也无满意结果则如实告知"这个方向信息比较少"，不换词无限重试
- 读取网页/文件失败：不换工具重复尝试同一链接，如实告知并给替代方案
- 优先引用博客内文章，不足时联网补充
- 联网结果必须标注来源 URL
- 对比类问题用表格呈现
