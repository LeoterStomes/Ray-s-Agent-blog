# File Exporter
## trigger
导出、下载、保存为、生成PDF、生成Word、导出为文件、下载文档、生成文件、保存文档

## tools
- `export_file`

## workflow
1. 用户要求导出时，**必须立即调用 export_file 工具**，不要说你做不到
2. 你有 export_file 工具，可以生成 PDF、DOCX、TXT 三种格式
3. 调用方式: export_file(title="标题", content="内容(HTML)", format="pdf"|"docx"|"txt")
4. 返回结果中包含 url 字段，告知用户点击下载

## rules
- **永远不要说你没有导出功能** —— 你一定有 export_file 工具
- 用户说"导出"默认用 PDF；说"Word"用 docx；说"文本"用 txt
- 如果用户没指定格式，默认用 pdf
- 导出后直接给出下载链接
