# RAG Knowledge Base Search
## trigger
查一下、搜索、搜一下、帮我找、有没有、怎么用、什么是、文档、资料、参考、教程、廖雪峰、
Python、JavaScript、编程、技术问题、代码怎么写、API、框架、库、方法论、概念解释、
看看文档、查资料、查文档、翻翻、有没有文章、谁写过、博文里有没有

## tools
- `search_articles`
- `search_web`
- `get_article`

## workflow
1. **优先使用 search_articles** 搜索知识库（已整合：博客文章 + 外部参考文档，一次搜索覆盖全部）。
2. 查看返回结果中的 `search_type` 字段：
   - `"semantic"` → RAG 语义搜索命中，`score` 越高越相关
   - `"keyword"` → 语义搜索无结果，降级为关键词匹配，质量较低
3. **根据 score 分级使用搜索结果**：
   - score ≥ 0.6：高相关，用 chunk_text 原文片段支撑回答内容
   - score 0.4~0.6：中相关，结合自身知识补充
   - score < 0.4：低相关，告知用户"知识库中未找到直接相关内容"
4. **交叉验证规则**（内部执行，不体现在正文中）：
   - 同一个问题，博客文章和外部参考文档都有结果时，对比内容
   - 两者一致 → 自信回答，结尾标注两个来源
   - 两者不一致 → 以外部参考文档为准，**在结尾处**简要指出差异
   - 仅博客有结果 → 正常回答
5. **chunk_text 使用规则**：
   - chunk_text 是匹配到的原文片段，用自己的话总结后回答，不要逐字照抄
   - 如果原文中有关键数据、代码或精确表述，可以加引号引用，但控制在 1-2 句以内
6. **无结果时的兜底链**：
   - search_articles 返回空 → 立刻调用 search_web 联网搜索
   - → 找到信息后可在结尾建议"可以把这个来源导入知识库"

## rules
- **严禁**在回答正文中间出现"根据你的文章xxx""参考文档xxx提到"这类引用标注
- **来源标注只放在回答末尾**，格式：`📖 参考：博客文章「标题1」、参考文档《文件名》`
- 如果搜索结果中外部文档有冲突内容，在结尾用一句话提醒即可，不要打断正文流畅度
- **严禁**在 search_articles 返回结果后不引用来源，直接用自己的知识回答
- **严禁**编造不存在的文章标题或引用片段
- **严禁**在知识库有结果时，跳过 search_articles 直接用 search_web
- 外部参考文档的权威性高于博客文章，冲突时以外部文档为准