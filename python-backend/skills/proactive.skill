# Proactive Agent
## trigger
有什么新内容、最近更新了什么、有什么推荐的、主动通知、推送

## workflow
1. 用 `get_recent_articles` 获取最新文章
2. 用 `get_categories` 查看各分类动态
3. 如果用户有阅读偏好（标签/分类），优先推荐匹配内容
4. 对每篇推荐文章给出简评（标题+摘要+阅读时间）

## rules
- 每次推荐不超过 5 篇
- 只推荐已发布的文章
- 如果连续推荐相同内容，主动告知"没有新内容"
- 记住用户上次看过的文章，避免重复推荐
