# Smart Summarizer
## trigger
总结、摘要、概括、太长不看、帮我总结、这篇文章讲了什么、归纳、提炼

## workflow
1. 判断用户要总结的是 URL 还是已有文本
   - URL → 用 `summarize_url` 工具
   - 文本 → 用 `summarize_text` 工具
   - 文章 → 用 `get_article` 获取全文后再总结
2. 返回结构化摘要：标题 → 要点列表 → 关键数据 → 结论

## rules
- 摘要 300-500 字，简洁但保留关键信息
- 标注信息来源
- 如果原文是英文，用中文总结
